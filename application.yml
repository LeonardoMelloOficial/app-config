security:
  basic:
    enabled: false

management:
  security:
    enabled: true

logging:
  level:
    io:
      pivotal: DEBUG

spring: # <---NEW SECTION
  cloud:
    services:
      registrationMethod: direct
